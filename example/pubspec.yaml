name: example
description: "A new Flutter project."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  fake_firebase_remote_config:
    path: ../
  firebase_remote_config: ^6.1.4
  flutter:
    sdk: flutter
